@model SistemaJobs.Candidato

@{
    ViewBag.Title = "Create";
}

<h2 style="margin-top:70px; color: #385898;">Qual vaga deseja?</h2>
<hr class="hr-custom" />

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    @Html.ValidationSummary(true)
    <input type="hidden" value="@(ViewBag.IdFuncionario)" name="IdFuncionario" />
    <input type="hidden" value="@(ViewBag.IdVagaProjeto)" name="IdVagaProjeto" />

    <div class="row">
        <div class="col-md-12">

            @foreach (var p in ViewBag.ListaCargos)
            {
                <input type="radio" name="Cargo" value="@(p.Nome)"><label class="lbl-cargos">@p.Nome</label><br>
            }

        </div>
    </div>

    @*<div class="form-group">
            @Html.LabelFor(model => model.IdVagaProjeto, "IdVagaProjeto", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("IdVagaProjeto", String.Empty)
                @Html.ValidationMessageFor(model => model.IdVagaProjeto)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IdFuncionario, "IdFuncionario", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("IdFuncionario", String.Empty)
                @Html.ValidationMessageFor(model => model.IdFuncionario)
            </div>
        </div>*@

    <div class="form-group">
        <div class="col-md-12" style="margin-top:20px;">
            @Html.ActionLink("Voltar | ", "Index")
            <input type="submit" value="Enviar" class="btn btn-default" />
        </div>
    </div>
</div>
}
